<% @nav = 'order_audit' %>
<%= render 'shared/header' %>
<div class="mainbar">

<%= render 'shared/content_header' %>

  <div class="matter">
    <div class="container">

      <div class="row">
        <div class="col-xs-12">
          <!-- PAGE CONTENT BEGINS -->


          <div class="row">
            <div class="col-xs-12">
              <h3>用户端首页顶部大图</h3>

              <div class="table-responsive">
                <table id="sample-table-2" class="table table-striped table-bordered table-hover">
                  <thead>
    <tr>
      <th class="hidden-480">图片</th>
      <th class="hidden-480">名称</th>
      <th class="hidden-480">描述</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @viewimages.each do |viewimage| %>
      <tr>
        <td><%= image_tag viewimage.image.url(:thumb) %></td>
        <td class="hidden-480"><%= viewimage.title %></td>
        <td class="hidden-480"><%= viewimage.content %></td>
        <td><%= link_to '显示', userwebs_path %></td>
        <td><%= link_to '删除', viewimage, method: :delete, data: { confirm: '你确定删除吗?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
            </div>
          </div>
<%= link_to '新建', new_viewimage_path, class: "btn btn-info btn-sm" %>
        </div><!-- /.col -->
      </div><!-- /.row -->
    </div>
  </div>
  <div class="clearfix"></div>
<br>
</div>

<%= render 'shared/footer' %>
