  <!-- Page heading -->
  <div class="page-head">
    <h2 class="pull-left"><i class="icon-home"></i> <%= params[:action] %></h2>

    <!-- Breadcrumb -->
    <div class="bread-crumb pull-right">
      <a href="/welcome"><i class="icon-home"></i> 首页</a> 
      <!-- Divider -->
      <span class="divider">/</span> 
      <a href="#" class="bread-current"><%= params[:action] %></a>
    </div>

    <div class="clearfix"></div>

  </div>
  <!-- Page heading ends -->
  <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <% end %>